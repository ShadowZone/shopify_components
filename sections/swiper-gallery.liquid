<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js" defer></script>

<style>
  /* Grundlegendes Layout der Section */
  .section-{{ section.id }} {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    background-color: {{ section.settings.bg_color }};
    overflow: hidden;
  }

  .swiper-container-{{ section.id }} {
    width: 100%;
    padding-bottom: 40px;
  }

  /* Slide Styling (Container) */
  .swiper-slide {
    width: 70%; 
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    opacity: 0.6;
    transition: all 0.4s ease;
    user-select: none;
    -webkit-user-drag: none;
    -webkit-touch-callout: none;
    cursor: grab;
    -webkit-tap-highlight-color: transparent; 
  }
   
  /* Skalierung des Inhalts (Link) für inaktive Slides */
  .swiper-slide .slide-content-link {
    transform: scale(0.9);
    transition: transform 0.4s ease, opacity 0.4s ease;
  }

  /* Media Query für Desktop */
  @media(min-width: 768px) {
    .swiper-slide {
      width: 40%;
    }
  }

  /* Das AKTIVE (zentrierte) Element */
  .swiper-slide-active {
    opacity: 1;
    z-index: 2;
  }
  
  /* Skalierung des Inhalts (Link) des aktiven Slides */
  .swiper-slide-active .slide-content-link {
    transform: scale(1.0);
    opacity: 1;
  }
  

  /* Inhalt Wrapper (Link) */
  .slide-content-link {
    text-decoration: none;
    color: inherit;
    display: block;
    width: 100%;
    border-radius: {{ section.settings.border_radius }}px;
    overflow: hidden;
  }

  /* Bild Styling */
  .slide-image {
    width: 100%;
    height: 350px;
    object-fit: cover;
    display: block;
  }

  /* Caption Styling */
  .slide-caption {
    text-align: center;
    padding: 15px 10px;
    font-size: 1.1rem;
    font-weight: 600;
    background: {{ section.settings.caption_bg_color }};
    color: {{ section.settings.caption_text_color }};
    font-family: {{ section.settings.caption_font_family }};
  }
</style>

<div class="section-{{ section.id }}">
  <div class="swiper swiper-container-{{ section.id }}">
    <div class="swiper-wrapper">
      {% for block in section.blocks %}
        <div class="swiper-slide" {{ block.shopify_attributes }}>
          {% if block.settings.link != blank %}
            <a href="{{ block.settings.link }}" class="slide-content-link">
          {% else %}
            <div class="slide-content-link">
          {% endif %}
            
            {% if block.settings.image != blank %}
              {{ block.settings.image | image_url: width: 800 | image_tag: 
                loading: 'lazy', 
                class: 'slide-image', 
                alt: block.settings.caption 
              }}
            {% else %}
              {{ 'image' | placeholder_svg_tag: 'slide-image' }}
            {% endif %}

            {% if block.settings.caption != blank %}
              <div class="slide-caption">
                {{ block.settings.caption }}
              </div>
            {% endif %}

          {% if block.settings.link != blank %}
            </a>
          {% else %}
            </div>
          {% endif %}
        </div>
      {% endfor %}
    </div>
    
    {%if settings.pagination %}<div class="swiper-pagination"></div> {% endif %}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const swiper = new Swiper('.swiper-container-{{ section.id }}', {
      effect: 'slide',
      grabCursor: true,
      centeredSlides: true,
      slidesPerView: 'auto',
      spaceBetween: 20,
      loop: false,
      speed: 600,
      
      pagination: {
        el: '.swiper-pagination',
        clickable: true,
      },
      
      // Accessibility
      a11y: {
        prevSlideMessage: 'Previous slide',
        nextSlideMessage: 'Next slide',
      },
    });
  });
</script>

{% schema %}
{
  "name": "Swiper Highlight Slider",
  "settings": [
    {
      "type": "header",
      "content": "Allgemeine Einstellungen"
    },
    {
      "type": "range",
      "id": "border_radius",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "label": "Abgerundete Ecken der Slides",
      "default": 12
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Hintergrundfarbe Sektion",
      "default": "#f4f4f4"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Abstand oben",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Abstand unten",
      "default": 40
    },
    {
      "type": "header",
      "content": "Pagination"
    },
    {
      "type": "checkbox",
      "id": "pagination",
      "label": "Pagination anzeigen",
      "default": true
    },
    {
      "type": "header",
      "content": "Caption (Titel) Einstellungen"
    },
    {
      "type": "select",
      "id": "caption_font_family",
      "label": "Schriftart Caption",
      "default": "Helvetica, Arial, sans-serif",
      "options": [
        { "value": "Helvetica, Arial, sans-serif", "label": "Standard (Helvetica/Arial)" },
        { "value": "Georgia, serif", "label": "Serif (Georgia)" },
        { "value": "Verdana, Geneva, sans-serif", "label": "Sans-Serif (Verdana)" },
        { "value": "monospace", "label": "Monospace" }
      ]
    },
    {
      "type": "color",
      "id": "caption_text_color",
      "label": "Schriftfarbe Caption",
      "default": "#333333"
    },
    {
      "type": "color",
      "id": "caption_bg_color",
      "label": "Hintergrundfarbe Caption (für Transparent: #00000000)",
      "default": "#ffffff"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Bild"
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Überschrift",
          "default": "Hier steht dein Text"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link (URL)"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Swiper Highlight Slider",
      "blocks": [
        { "type": "slide" },
        { "type": "slide" },
        { "type": "slide" }
      ]
    }
  ]
}
{% endschema %}
